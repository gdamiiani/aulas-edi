int criptografar(char palavra1[], char palavra2[], char textoCriptografado[]);

void descriptografar(char textoCriptografado[], int tamanho, char palavra1[], char palavra2[]);

int criptografar(char palavra1[], char palavra2[], char textoCriptografado[]) {
    int i = 0;
    int j = 0;

    for (i = 0; palavra1[i] != '\0'; ++i);
    for (j = 0; palavra1[i] != '\0'; ++j);

    int size = i + j;

    for (int k; k
    textoCriptografado

    return size;
}

void descriptografar(char textoCriptografado[], int tamanho, char palavra1[], char palavra2[]);
